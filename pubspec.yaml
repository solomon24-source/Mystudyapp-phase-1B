name: mystudyapp
description: MyStudyApp Phase 1B - Auth
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  dio: ^5.5.0
  flutter_secure_storage: ^9.2.2
  go_router: ^14.6.2
  flutter_dotenv: ^5.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env.dev
